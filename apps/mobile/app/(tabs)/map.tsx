import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function MapScreen() {
  return (
    <PlaceholderScreen
      title="Map"
      description="MapLibre-based discovery with marker clustering arrives in Phase 2."
    />
  );
}
