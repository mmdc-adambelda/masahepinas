import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function SavedScreen() {
  return (
    <PlaceholderScreen
      title="Saved"
      description="Your saved and favorited spas will show up here once listings exist (Phase 2)."
    />
  );
}
